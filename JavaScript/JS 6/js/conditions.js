// let num = +prompt("Enter number:");

// if(num < 0) alert("Number is negative.");

// if(num < 0) alert("Number is negative."), alert("Number is less than zero.");

// if(num < 0) {
// 	alert("Number is negative.");
// 	alert("Number is less than zero.");
// };

// if(0) {
// 	alert("Number is negative.");
// };

// if(" ") {
// 	alert("Number is negative.");
// };

// if(num < 0) {
// 	alert("Number is negative.");
// };

// if(num > 0) {
// 	alert("Number is positive.");
// };

// if(num < 0) {
// 	alert("Number is negative.");
// } else if(num > 0) {
//   alert("Number is positive.");
// } else if(num == 0) {
// 	alert("Number is neutral.");
// } else {
// 	alert("You have not entered a number.");
// };

// Condition with question mark: ?
// let age = +prompt("Enter your age:");
// let accesAllowed;

// if(age >= 18) {
// 	accesAllowed = true;
// } else {
// 	accesAllowed = false;
// };

// if(accesAllowed) {
// 	alert("You are allowed.");
// } else {
// 	alert("You are not allowed!");
// }

//Syntax
// condition ? true case : false case;

// accesAllowed = age >= 18 ? "Adult" : "Child";
// accesAllowed = age >= 18 ? true : false;

// accesAllowed ? alert("You are allowed") : alert("You are not allowed!");

// let name = prompt("Enter your name:");
// let user;

// if(name == "John") {
// 	user = "Admin";
// } else {
// 	user = "User";
// }

let num = prompt("Enter number:");

// num < 0 ? alert("Negative") : num > 0 ? alert("Positive") : num === 0 ? alert("Zero") : alert("Not a number");

alert(num < 0 ? "Negative" : num > 0 ? "Positive" : num === 0 ? "Zero" : "Not a number");

alert(age >= 18 ? "Adult" : "Child")